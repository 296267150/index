$(document).ready(function() {
	$('.textEffect1').funnyText({
		speed: 700,
		activeColor: '#fff',
		color: 'black',
		fontSize: '6em'
	});
	$('.textEffect2').funnyText({
		speed: 700,
		fontSize: '4em',
		color: '#191919',
		activeColor: '#fff',
		borderColor: 'black'
	});
});